// number 
let age: number = 23;

// string
let friendsName: string = "saif";

// boolean
let isMarried: boolean = false;

// null
let companyName: null = null;

//undefined
let whereIsTheKey: undefined = undefined;

// Array
let friendsNamesAre: string[] = ['ashik', 'ali', 'nahin', 'rayan', 'ashraf'];;

// tuple // which represents array, where array length is fixed so does types
let matchAndScores: [string, number, number] = ['rmaVSbayern', 2, 2]

let a: string;
a = 'wer';
// a = 123;  error
// a = true error